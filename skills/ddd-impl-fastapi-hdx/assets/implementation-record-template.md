# Implementation record: `<increment-id>`

## Increment identity

- Increment id: `<increment-id>`
- Handoff: `docs/ddd/implementation-handoff.md`
- Verified authority revisions:
  - `<path>` — `sha256:<64 hex>` — sections: `<H2, H2>`

## Preflight

- `hdx-domain-kit` dependency: `<found|missing>` (`<version>`)
- Composition root: `<module path or "not found">`
- Resolved `target.placement`: `<domain package path>`

## Technical plan

| In-scope behavior | Target file/module | hdx-domain-kit construct |
| --- | --- | --- |
| `<behavior>` | `<path>` | `<construct>` |

## Trace

- Files touched: `<path list>`
- Checks run: `<command>` — `<pass/fail>`
- Gated items:
  - `<migration/composition-root/dependency change>` — `<proposed|approved|deferred>`
- Deviations from the handoff: `<none, or exact deviation and reason>`

## Result

`<implementation complete for this increment, or a bounded stop naming the exact blocker and return_on_conflict target>`
